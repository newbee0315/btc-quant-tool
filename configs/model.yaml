model:
  type: "random_forest"
  parameters:
    n_estimators: 100
    max_depth: 10
  target_windows: [10, 30, 60] # minutes

data:
  symbol: "BTC/USDT"
  timeframe: "1m"
  history_days: 30

trading:
  risk_limit: 0.02
  stop_loss: 0.01
